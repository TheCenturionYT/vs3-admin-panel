---
phase: 1
plan: 04
title: "App Shell — Sidebar, Topbar, Stub Pages (Dashboard, Server Log)"
wave: 2
depends_on: [01]
files_modified:
  - vs3-panel/src/routes/(staff)/+layout.svelte
  - vs3-panel/src/routes/(staff)/dashboard/+page.svelte
  - vs3-panel/src/routes/(staff)/server-log/+page.svelte
  - vs3-panel/src/lib/components/AppSidebar.svelte
  - vs3-panel/src/lib/components/AppTopbar.svelte
autonomous: true
requirements:
  - UX-01
  - UX-02
  - AUTH-05

must_haves:
  truths:
    - "The (staff) layout renders a fixed 240px sidebar and 48px topbar using the VS3 medieval gold palette"
    - "The sidebar shows all Phase 1 nav items — active links for Dashboard, Staff Management, Server Log; disabled items for Phase 2/3 features with correct tooltip text"
    - "The current route is visually highlighted in the sidebar with the gold left-border pattern"
    - "The topbar shows the logged-in username, the correct role badge (gold for Head Admin, muted for Staff), and a Sign Out button"
    - "Dashboard and Server Log stub pages render the 'Coming in Phase 2' placeholder card"
  artifacts:
    - path: "vs3-panel/src/routes/(staff)/+layout.svelte"
      provides: "Staff app shell — sidebar + topbar + main content slot"
      contains: "AppSidebar"
    - path: "vs3-panel/src/lib/components/AppSidebar.svelte"
      provides: "Sidebar nav with active route detection and disabled Phase 2/3 items"
      contains: "Coming in Phase 2"
    - path: "vs3-panel/src/lib/components/AppTopbar.svelte"
      provides: "Topbar with username, role badge, logout"
      contains: "Head Admin"
    - path: "vs3-panel/src/routes/(staff)/dashboard/+page.svelte"
      provides: "Dashboard stub page"
      contains: "Dashboard coming in Phase 2"
    - path: "vs3-panel/src/routes/(staff)/server-log/+page.svelte"
      provides: "Server Log stub page"
      contains: "Server Log coming in Phase 2"
  key_links:
    - from: "vs3-panel/src/routes/(staff)/+layout.svelte"
      to: "vs3-panel/src/routes/(staff)/+layout.server.ts"
      via: "$props() data.user — username and role from the server load function"
      pattern: "data.user"
    - from: "vs3-panel/src/lib/components/AppSidebar.svelte"
      to: "$page.url.pathname"
      via: "Active route detection using SvelteKit $page store"
      pattern: "page.url.pathname"
---

<objective>
Build the (staff) route group app shell: the sidebar component with all Phase 1 nav items, the topbar component with username/role badge/logout, the staff layout that wires them together, and the two stub pages (Dashboard and Server Log).

Purpose: Plan 05 (Staff Management) slots into this shell. All Phase 2+ pages also slot here. Getting the shell right in Phase 1 means no layout work in subsequent phases.

Output: The visual frame of the admin panel — sidebar, topbar, content area. Staff Management is still a stub at this point; Plan 05 fills it in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Exact specs from UI-SPEC — executor uses these directly, no exploration needed -->

From UI-SPEC Surface 2 — Sidebar:
  Width: 240px, fixed left, full height
  Background: #231d14 (bg-card)
  Right border: 1px solid #3d3426
  Nav item height: 44px min-height, 16px horizontal padding, 14px text
  Active item: text-primary (#c4a45a), left border 2px solid #c4a45a, background rgba(196,164,90,0.08)
  Inactive item text: #8b7d65 (text-muted-foreground)
  Inactive hover: text #d4c5a0, background rgba(212,193,160,0.04)
  Disabled items: text-muted, cursor-not-allowed, no hover
  Section labels: 11px/600, uppercase, letter-spacing 0.07em, gold #c4a45a, 8px vertical padding
  Separator: #3d3426, 1px, 8px vertical margin

From UI-SPEC Surface 2 — Topbar:
  Height: 48px, fixed top, full width
  Background: #231d14
  Bottom border: 1px solid #3d3426
  Left: "VS3 Panel" 15px/600/gold, letter-spacing 0.05em
  Right: username (14px/400/foreground) + role badge + logout button (ghost)
  Head Admin badge: rgba(196,164,90,0.2) bg, rgba(196,164,90,0.3) border, text #c4a45a, 11px/600
  Staff badge: rgba(139,125,101,0.15) bg, border #3d3426, text #8b7d65, 11px/600
  Logout button: ghost, text-muted, hover text-foreground

Sidebar nav items in order (from UI-SPEC):
  1. Dashboard      → /dashboard       — active link, icon: LayoutDashboard
  2. Staff Mgmt     → /staff-management — active link, icon: Users
  3. Server Log     → /server-log      — active link, icon: ScrollText
  4. [separator]
  5. Factions       → disabled, "Coming in Phase 2", icon: Shield
  6. Nodes          → disabled, "Coming in Phase 2", icon: MapPin
  7. Wars           → disabled, "Coming in Phase 2", icon: Swords
  8. Diplomacy      → disabled, "Coming in Phase 2", icon: Handshake
  9. SP Catalogue   → disabled, "Coming in Phase 2", icon: BookOpen
  10. [separator]
  11. Upkeep        → disabled, "Coming in Phase 3", icon: CalendarClock
  12. Metrics       → disabled, "Coming in Phase 3", icon: BarChart3

Icon size: 16px (w-4 h-4), stroke-width 1.5

Layout: Topbar is 48px fixed at top. Sidebar is fixed left, starts at 48px from top (below topbar), full height remaining. Main content area has padding-left 240px and padding-top 48px.

From UI-SPEC Surface 4 — Dashboard Stub:
  Page heading: "Dashboard" (22px/600/foreground)
  Sub-heading: "Server overview and health metrics" (14px/400/muted)
  Card heading: "Dashboard coming in Phase 2" (15px/600/foreground, centered)
  Card body: "Faction health, node status, and upkeep metrics will appear here once Phase 2 is complete." (14px/400/muted, centered, max-width 400px)
  Icon: Construction (32px, muted)

From UI-SPEC Surface 5 — Server Log Stub:
  Page heading: "Server Log" (22px/600/foreground)
  Sub-heading: "Chronological record of all server events" (14px/400/muted)
  Card heading: "Server Log coming in Phase 2" (15px/600/foreground, centered)
  Card body: "All faction, node, war, and upkeep events will be recorded here once Phase 2 is complete." (14px/400/muted)
  Icon: Construction (same)

data.user from +layout.server.ts: { id: string, username: string, role: 'head_admin' | 'staff' }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppSidebar and AppTopbar components</name>
  <read_first>
    - vs3-panel/src/lib/components/ (list directory — see what shadcn-svelte created)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Surface 2: App Shell — Sidebar + Topbar, full spec)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Buttons section — ghost variant spec)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Badges section — role badge specs)
  </read_first>
  <files>
    vs3-panel/src/lib/components/AppSidebar.svelte
    vs3-panel/src/lib/components/AppTopbar.svelte
  </files>
  <action>
Create vs3-panel/src/lib/components/AppSidebar.svelte:

```svelte
<script lang="ts">
  import { page } from '$app/stores';
  import {
    LayoutDashboard,
    Users,
    ScrollText,
    Shield,
    MapPin,
    Swords,
    Handshake,
    BookOpen,
    CalendarClock,
    BarChart3
  } from '@lucide/svelte';

  // Nav items with metadata
  const activeNavItems = [
    { label: 'Dashboard',        href: '/dashboard',         Icon: LayoutDashboard },
    { label: 'Staff Management', href: '/staff-management',  Icon: Users },
    { label: 'Server Log',       href: '/server-log',        Icon: ScrollText },
  ];

  const phase2Items = [
    { label: 'Factions',    Icon: Shield },
    { label: 'Nodes',       Icon: MapPin },
    { label: 'Wars',        Icon: Swords },
    { label: 'Diplomacy',   Icon: Handshake },
    { label: 'SP Catalogue', Icon: BookOpen },
  ];

  const phase3Items = [
    { label: 'Upkeep',  Icon: CalendarClock },
    { label: 'Metrics', Icon: BarChart3 },
  ];

  function isActive(href: string): boolean {
    return $page.url.pathname === href || $page.url.pathname.startsWith(href + '/');
  }
</script>

<nav
  class="fixed left-0 top-[48px] bottom-0 w-[240px] overflow-y-auto"
  style="background: #231d14; border-right: 1px solid #3d3426;"
  aria-label="Main navigation"
>
  <!-- Active nav items -->
  <div class="py-2">
    {#each activeNavItems as item}
      <a
        href={item.href}
        class="flex items-center gap-3 px-4 min-h-[44px] text-[14px] transition-colors relative
               {isActive(item.href)
                 ? 'text-primary'
                 : 'text-muted-foreground hover:text-foreground'}"
        style={isActive(item.href)
          ? 'border-left: 2px solid #c4a45a; background: rgba(196,164,90,0.08); padding-left: 14px;'
          : 'border-left: 2px solid transparent;'}
      >
        <item.Icon class="w-4 h-4 shrink-0" strokeWidth={1.5} />
        <span>{item.label}</span>
      </a>
    {/each}
  </div>

  <!-- Separator -->
  <div class="mx-4 my-2" style="border-top: 1px solid #3d3426;"></div>

  <!-- Phase 2 section label -->
  <div
    class="px-4 py-2 text-[11px] font-semibold uppercase tracking-[0.07em] text-primary"
  >
    Phase 2
  </div>

  <!-- Phase 2 disabled items -->
  <div class="pb-2">
    {#each phase2Items as item}
      <div
        class="flex items-center gap-3 px-4 min-h-[44px] text-[14px] text-muted-foreground cursor-not-allowed select-none"
        title="Coming in Phase 2"
        aria-disabled="true"
      >
        <item.Icon class="w-4 h-4 shrink-0 opacity-50" strokeWidth={1.5} />
        <span class="opacity-60">{item.label}</span>
        <span
          class="ml-auto text-[10px] font-semibold uppercase tracking-wide opacity-40"
        >
          Soon
        </span>
      </div>
    {/each}
  </div>

  <!-- Separator -->
  <div class="mx-4 my-2" style="border-top: 1px solid #3d3426;"></div>

  <!-- Phase 3 section label -->
  <div
    class="px-4 py-2 text-[11px] font-semibold uppercase tracking-[0.07em] text-primary"
  >
    Phase 3
  </div>

  <!-- Phase 3 disabled items -->
  <div class="pb-2">
    {#each phase3Items as item}
      <div
        class="flex items-center gap-3 px-4 min-h-[44px] text-[14px] text-muted-foreground cursor-not-allowed select-none"
        title="Coming in Phase 3"
        aria-disabled="true"
      >
        <item.Icon class="w-4 h-4 shrink-0 opacity-50" strokeWidth={1.5} />
        <span class="opacity-60">{item.label}</span>
        <span
          class="ml-auto text-[10px] font-semibold uppercase tracking-wide opacity-40"
        >
          Soon
        </span>
      </div>
    {/each}
  </div>
</nav>
```

Create vs3-panel/src/lib/components/AppTopbar.svelte:

```svelte
<script lang="ts">
  import { enhance } from '$app/forms';
  import { Loader2 } from '@lucide/svelte';

  let {
    username,
    role
  }: {
    username: string;
    role: 'head_admin' | 'staff';
  } = $props();

  let loggingOut = $state(false);

  const roleLabel = role === 'head_admin' ? 'Head Admin' : 'Staff';
</script>

<header
  class="fixed top-0 left-0 right-0 h-[48px] flex items-center justify-between px-4 z-10"
  style="background: #231d14; border-bottom: 1px solid #3d3426;"
>
  <!-- Brand -->
  <span
    class="text-[15px] font-semibold text-primary tracking-[0.05em]"
  >
    VS3 Panel
  </span>

  <!-- Right cluster -->
  <div class="flex items-center gap-3">
    <!-- Username -->
    <span class="text-[14px] text-foreground">{username}</span>

    <!-- Role badge -->
    {#if role === 'head_admin'}
      <span
        class="px-2 py-1 rounded-full text-[11px] font-semibold"
        style="background: rgba(196,164,90,0.2); border: 1px solid rgba(196,164,90,0.3); color: #c4a45a;"
      >
        Head Admin
      </span>
    {:else}
      <span
        class="px-2 py-1 rounded-full text-[11px] font-semibold"
        style="background: rgba(139,125,101,0.15); border: 1px solid #3d3426; color: #8b7d65;"
      >
        Staff
      </span>
    {/if}

    <!-- Logout button -->
    <form
      method="POST"
      action="/login?/logout"
      use:enhance={() => {
        loggingOut = true;
        return async ({ update }) => {
          await update();
          loggingOut = false;
        };
      }}
    >
      <button
        type="submit"
        disabled={loggingOut}
        class="flex items-center gap-1 px-3 py-1 rounded-md text-[14px] text-muted-foreground
               hover:text-foreground transition-colors disabled:opacity-50"
      >
        {#if loggingOut}
          <Loader2 class="w-3 h-3 animate-spin" />
        {/if}
        Sign Out
      </button>
    </form>
  </div>
</header>
```
  </action>
  <verify>
    <automated>
      cd vs3-panel && grep -c "Coming in Phase 2" src/lib/components/AppSidebar.svelte
    </automated>
  </verify>
  <acceptance_criteria>
    - vs3-panel/src/lib/components/AppSidebar.svelte exists and contains "Coming in Phase 2"
    - AppSidebar.svelte contains all 10 nav items: Dashboard, Staff Management, Server Log, Factions, Nodes, Wars, Diplomacy, SP Catalogue, Upkeep, Metrics
    - AppSidebar.svelte contains "$page.url.pathname" (active route detection)
    - AppSidebar.svelte contains "border-left: 2px solid #c4a45a" (active item gold border)
    - AppSidebar.svelte contains "cursor-not-allowed" (disabled items)
    - vs3-panel/src/lib/components/AppTopbar.svelte exists and contains "Head Admin"
    - AppTopbar.svelte contains "rgba(196,164,90,0.2)" (Head Admin badge background)
    - AppTopbar.svelte contains "Sign Out" (exact copy from UI-SPEC copywriting contract)
    - AppTopbar.svelte contains "use:enhance" on the logout form
  </acceptance_criteria>
  <done>AppSidebar shows all Phase 1 nav items with correct active/disabled states and VS3 styling. AppTopbar shows username, role badge, and logout button matching UI-SPEC Surface 2 exactly.</done>
</task>

<task type="auto">
  <name>Task 2: (staff) layout.svelte and stub pages for Dashboard and Server Log</name>
  <read_first>
    - vs3-panel/src/routes/(staff)/+layout.server.ts (from Plan 03 — verify data.user shape)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Surface 3: layout notes, Surface 4: Dashboard Stub, Surface 5: Server Log Stub)
    - vs3-panel/src/lib/components/AppSidebar.svelte (just created — verify it exists before importing)
    - vs3-panel/src/lib/components/AppTopbar.svelte (just created — verify it exists before importing)
  </read_first>
  <files>
    vs3-panel/src/routes/(staff)/+layout.svelte
    vs3-panel/src/routes/(staff)/dashboard/+page.svelte
    vs3-panel/src/routes/(staff)/server-log/+page.svelte
  </files>
  <action>
Create vs3-panel/src/routes/(staff)/+layout.svelte:

```svelte
<script lang="ts">
  import AppSidebar from '$lib/components/AppSidebar.svelte';
  import AppTopbar from '$lib/components/AppTopbar.svelte';
  import type { LayoutData } from './$types';

  let { data, children }: { data: LayoutData; children: import('svelte').Snippet } = $props();
</script>

<!-- Fixed topbar -->
<AppTopbar username={data.user.username} role={data.user.role} />

<!-- Fixed sidebar (positioned below topbar via top-[48px] in component) -->
<AppSidebar />

<!-- Main content area — offset for sidebar and topbar -->
<main class="pl-[240px] pt-[48px] min-h-screen bg-background">
  <div class="p-6">
    {@render children()}
  </div>
</main>
```

Create vs3-panel/src/routes/(staff)/dashboard/+page.svelte (Dashboard stub):

```svelte
<script lang="ts">
  import { Construction } from '@lucide/svelte';
</script>

<svelte:head>
  <title>Dashboard — VS3 Panel</title>
</svelte:head>

<!-- Page heading -->
<div class="mb-6">
  <h1 class="text-[22px] font-semibold text-foreground">Dashboard</h1>
  <p class="text-[14px] text-muted-foreground mt-1">Server overview and health metrics</p>
</div>

<!-- Stub card -->
<div class="bg-card border border-border rounded-md p-6 flex flex-col items-center gap-4 py-12">
  <Construction class="w-8 h-8 text-muted-foreground" strokeWidth={1.5} />
  <h2 class="text-[15px] font-semibold text-foreground text-center">
    Dashboard coming in Phase 2
  </h2>
  <p class="text-[14px] text-muted-foreground text-center max-w-[400px]">
    Faction health, node status, and upkeep metrics will appear here once Phase 2 is complete.
  </p>
</div>
```

Create vs3-panel/src/routes/(staff)/server-log/+page.svelte (Server Log stub):

```svelte
<script lang="ts">
  import { Construction } from '@lucide/svelte';
</script>

<svelte:head>
  <title>Server Log — VS3 Panel</title>
</svelte:head>

<!-- Page heading -->
<div class="mb-6">
  <h1 class="text-[22px] font-semibold text-foreground">Server Log</h1>
  <p class="text-[14px] text-muted-foreground mt-1">Chronological record of all server events</p>
</div>

<!-- Stub card -->
<div class="bg-card border border-border rounded-md p-6 flex flex-col items-center gap-4 py-12">
  <Construction class="w-8 h-8 text-muted-foreground" strokeWidth={1.5} />
  <h2 class="text-[15px] font-semibold text-foreground text-center">
    Server Log coming in Phase 2
  </h2>
  <p class="text-[14px] text-muted-foreground text-center max-w-[400px]">
    All faction, node, war, and upkeep events will be recorded here once Phase 2 is complete.
  </p>
</div>
```

Also create the staff-management directory stub so Plan 05 has a place to slot in. Create vs3-panel/src/routes/(staff)/staff-management/ directory (may need a minimal +page.svelte placeholder):

```svelte
<!-- vs3-panel/src/routes/(staff)/staff-management/+page.svelte -->
<!-- Placeholder — replaced in full by Plan 05 -->
<script lang="ts">
  // Staff Management page — see Plan 05 for implementation
</script>
<p class="text-muted-foreground text-[14px]">Loading Staff Management...</p>
```
  </action>
  <verify>
    <automated>
      cd vs3-panel && grep -c "Dashboard coming in Phase 2" "src/routes/(staff)/dashboard/+page.svelte"
    </automated>
  </verify>
  <acceptance_criteria>
    - vs3-panel/src/routes/(staff)/+layout.svelte contains "AppSidebar" and "AppTopbar"
    - vs3-panel/src/routes/(staff)/+layout.svelte contains "data.user.username" and "data.user.role"
    - vs3-panel/src/routes/(staff)/+layout.svelte contains "pl-[240px] pt-[48px]" (content area offsets)
    - vs3-panel/src/routes/(staff)/dashboard/+page.svelte contains "Dashboard coming in Phase 2" (exact string from UI-SPEC copywriting contract)
    - vs3-panel/src/routes/(staff)/dashboard/+page.svelte contains "Faction health, node status, and upkeep metrics will appear here once Phase 2 is complete."
    - vs3-panel/src/routes/(staff)/server-log/+page.svelte contains "Server Log coming in Phase 2" (exact string from UI-SPEC)
    - vs3-panel/src/routes/(staff)/staff-management/+page.svelte exists (Plan 05 placeholder)
    - Running `cd vs3-panel && npm run build` exits 0
  </acceptance_criteria>
  <done>The (staff) layout renders the sidebar and topbar correctly from data.user. Dashboard and Server Log stubs match UI-SPEC Surface 4 and 5 copywriting exactly. Staff Management placeholder exists for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.user from server → Svelte component | Role value passed to AppTopbar determines badge rendering — must come from server, not client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | AppTopbar role badge | mitigate | `data.user.role` comes from `locals.pb.authStore.record?.role` set by PocketBase during auth — the client cannot spoof this value because it comes from the server load function in +layout.server.ts |
| T-04-02 | Elevation of Privilege | Disabled sidebar items | accept | Disabled items are purely navigational UX — the actual enforcement is at the route guard and PocketBase API rule levels (Plans 03 and 02). Disabled nav items do not protect resources. |
| T-04-03 | Information Disclosure | Username in topbar | accept | Username is visible to the authenticated user only — (staff) route guard in Plan 03 ensures only authenticated staff reach this layout |
</threat_model>

<verification>
After both tasks complete:

1. `grep "AppSidebar" "vs3-panel/src/routes/(staff)/+layout.svelte"` returns a match
2. `grep "AppTopbar" "vs3-panel/src/routes/(staff)/+layout.svelte"` returns a match
3. `grep "Dashboard coming in Phase 2" "vs3-panel/src/routes/(staff)/dashboard/+page.svelte"` returns a match
4. `grep "Server Log coming in Phase 2" "vs3-panel/src/routes/(staff)/server-log/+page.svelte"` returns a match
5. `grep "Coming in Phase 2" vs3-panel/src/lib/components/AppSidebar.svelte` returns a match
6. `grep "cursor-not-allowed" vs3-panel/src/lib/components/AppSidebar.svelte` returns a match
7. `cd vs3-panel && npm run build` exits 0
</verification>

<success_criteria>
- UX-01: App shell uses the full VS3 medieval gold palette — dark backgrounds, gold accents, muted parchment text — matching UI-SPEC Surface 2 dimensions and color specs exactly
- Sidebar renders all 10 nav items, correctly marks Dashboard/Staff Management/Server Log as active links and all Phase 2/3 items as disabled
- Active route is highlighted with gold left-border and background tint
- Topbar shows username and correct role badge style (Head Admin = gold, Staff = muted)
- Dashboard and Server Log stubs use exact copywriting from UI-SPEC Copywriting Contract
- Build succeeds (adapter-node output)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` using the summary template.
Include: files created, confirmation that build passes, and a note about the Svelte 5 runes syntax used (children snippet, $props(), $state()) vs Svelte 4 syntax (in case any shadcn-svelte component required adjustment).
</output>

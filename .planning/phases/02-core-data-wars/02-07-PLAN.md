---
phase: 02-core-data-wars
plan: 07
type: execute
wave: 4
depends_on:
  - "02-05"
  - "02-06"
files_modified:
  - vs3-panel/src/routes/(staff)/server-log/+page.svelte
  - vs3-panel/src/routes/(staff)/server-log/+page.server.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-04

must_haves:
  truths:
    - "Server log shows filterable entries with time, event type badge, description, and actor"
    - "Staff can filter log by event type, faction, node, and free text search"
    - "Sort toggle switches between newest-first and oldest-first"
    - "Staff can add a manual free-text log entry via the 'Add Manual Entry' modal"
    - "Manual entries are logged with the staff member's username as actor (not 'System')"
  artifacts:
    - path: "vs3-panel/src/routes/(staff)/server-log/+page.svelte"
      provides: "Server log with filter bar, sortable table, Add Manual Entry modal"
      contains: "filteredLog"
    - path: "vs3-panel/src/routes/(staff)/server-log/+page.server.ts"
      provides: "load + addManualEntry action"
      contains: "addManualEntry"
  key_links:
    - from: "vs3-panel/src/routes/(staff)/server-log/+page.server.ts"
      to: "PocketBase server_log collection"
      via: "locals.pb.collection('server_log').getFullList()"
      pattern: "server_log"
---

<objective>
Upgrade the server-log stub page to a full filterable log view with manual entry capability.

Purpose: Central audit trail for all Phase 2 events. The JSVM hooks from plan 02-01 auto-populate this log; this plan adds the UI to read and search it, plus LOG-04 manual entry.
Output: 2 route files upgrading the Phase 1 server-log stub.
</objective>

<execution_context>
@C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/execute-plan.md
</execution_context>

<context>
@C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-RESEARCH.md
@C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-PATTERNS.md
@C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-UI-SPEC.md
</context>

<interfaces>
<!-- Event type badge text colors (UI-SPEC lines 566-575): -->
```
faction_change:      #88bbdd (info blue)
node_change:         #d4c060 (yellow-gold)
war_event:           #e07840 (orange)
diplomacy_event:     #c4a45a (gold)
ownership_transfer:  #e07840 (orange)
manual_entry:        #8b7d65 (muted)
```

<!-- Event type badge bg/border (same muted pill for all, text varies): -->
```svelte
const EVENT_BADGE_BG = 'rgba(139,125,101,0.12)';
const EVENT_BADGE_BORDER = '#3d3426';
const EVENT_TEXT_COLORS: Record<string, string> = {
  faction_change: '#88bbdd',
  node_change: '#d4c060',
  war_event: '#e07840',
  diplomacy_event: '#c4a45a',
  ownership_transfer: '#e07840',
  manual_entry: '#8b7d65'
};
```

<!-- server_log record shape (from Phase 2 schema in 02-01): -->
```typescript
interface LogEntry {
  id: string;
  event_type: string;
  description: string;
  related_faction: string;   // relation ID
  related_node: string;      // relation ID
  actor: string;             // username or 'System'
  created: string;           // ISO timestamp
  expand?: {
    related_faction?: { id: string; name: string; };
    related_node?: { id: string; name: string; };
  };
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Log — `+page.server.ts` + `+page.svelte`</name>
  <files>
    vs3-panel/src/routes/(staff)/server-log/+page.svelte,
    vs3-panel/src/routes/(staff)/server-log/+page.server.ts
  </files>
  <read_first>
    - `C:/Users/Kramer/Desktop/VS3/vs3-panel/src/routes/(staff)/server-log/+page.svelte` — read current stub content to understand what is being replaced
    - `C:/Users/Kramer/Desktop/VS3/vs3-panel/src/routes/(staff)/staff-management/+page.server.ts` — action pattern (for addManualEntry)
    - `C:/Users/Kramer/Desktop/VS3/vs3-panel/src/routes/(staff)/staff-management/+page.svelte` — modal and form pattern
    - `C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-UI-SPEC.md` — Surface 7: Server Log (lines 549-594)
    - `C:/Users/Kramer\Desktop\VS3\.planning\phases\02-core-data-wars\02-PATTERNS.md` — server-log/+page.svelte section (lines 673-691)
  </read_first>
  <behavior>
    - Load fetches all server_log entries with expand: 'related_faction,related_node', sort: '-created' (newest first default)
    - Also loads all factions and all nodes for filter dropdowns
    - addManualEntry validates description (string, min 10, max 1000), related_faction (optional), related_node (optional). Creates server_log record with event_type='manual_entry', actor=locals.pb.authStore.record?.username
    - Client-side filtering: event_type Select, faction Select, node Select, text search (debounced 300ms)
    - Sort toggle: newest-first / oldest-first reverses the filteredLog array
  </behavior>
  <action>
**`+page.server.ts`:**

Load: fetch server_log with `expand: 'related_faction,related_node'`, sort: '-created'. Fetch factions and nodes for filter dropdown options. Map log entries to typed objects including expand data.

`addManualEntry` action:
Zod schema: description (string min 10 max 1000), related_faction (optional string), related_node (optional string).
Creates server_log record:
```typescript
await locals.pb.collection('server_log').create({
  event_type: 'manual_entry',
  description: parsed.data.description,
  related_faction: parsed.data.related_faction || '',
  related_node: parsed.data.related_node || '',
  actor: locals.pb.authStore.record?.username ?? 'Staff'
});
```
Note: manual entry actor is the staff username from the auth store — NOT "System". This is the one case where actor is not "System" (see RESEARCH open question 1).

**`+page.svelte`:**

Replace the Phase 1 stub entirely.

Script block:
- `$state` for eventTypeFilter, factionFilter, nodeFilter, search, debouncedSearch, sortOrder ('desc' | 'asc'), showAddModal, saving
- `$effect` for debounce (300ms)
- `$effect` to close modal on form success
- `filteredLog` $derived:
  1. Filter by eventTypeFilter (if not 'all')
  2. Filter by factionFilter (match related_faction id)
  3. Filter by nodeFilter (match related_node id)
  4. Filter by debouncedSearch (description or actor contains search string)
  5. Sort: if sortOrder === 'asc', reverse the array

Page header: "Server Log" (22px/600) + "Chronological record of all server events" (14px/text-muted) + "Add Manual Entry" button (ghost variant — top-right).

Filter bar (full-width, bg-card, border-border, px-4 py-3, gap-3 flex):
- Search input (flex-1): placeholder "Search log entries..."
- Event Type Select (200px): "All Event Types" + 6 event types (human-readable labels)
- Faction Select (180px): "All Factions" + faction names
- Node Select (180px): "All Nodes" + node names
- Sort toggle: two buttons, "Newest First" (gold-outlined when active) / "Oldest First" (ghost)

Log table (no row click — entries not addressable):
Columns: Time | Type | Description | Actor
- Time: formatDate using date-fns, 11px/400/text-muted, narrow column: `format(new Date(entry.created), 'dd MMM yyyy HH:mm')`
- Type: event type badge — muted pill bg/border with type-specific text color per EVENT_TEXT_COLORS map. Display friendly label: "Faction Change", "Node Change", "War Event", "Diplomacy Event", "Ownership Transfer", "Manual Entry"
- Description: 14px/400/text-primary, wraps
- Actor: 14px/400/text-muted

Empty state (no entries): "No log entries yet" / "Events will appear here as factions, nodes, and wars are managed."
Empty state (filter active, no results): "No matching entries" / "Adjust your filters or search terms to find log entries."

Add Manual Entry modal (680px):
- Title: "Add Manual Log Entry"
- Sub-title: "Record a ruling, RP event, or staff note." (11px/400/text-muted)
- Entry Text textarea (required, min 10, max 1000)
- Related Faction Select (optional, all factions + blank)
- Related Node Select (optional, all nodes + blank)
- "Add Entry" gold button

Use `use:enhance` on the form. `reset: false` to preserve values on validation error.
  </action>
  <verify>
    <automated>cd /c/Users/Kramer/Desktop/VS3/vs3-panel && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -i "server-log" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "addManualEntry" vs3-panel/src/routes/(staff)/server-log/+page.server.ts` returns at least 2
    - `grep -c "manual_entry" vs3-panel/src/routes/(staff)/server-log/+page.server.ts` returns at least 1
    - `grep -c "authStore.record.*username" vs3-panel/src/routes/(staff)/server-log/+page.server.ts` returns at least 1 (actor = staff username)
    - `grep -c "filteredLog" vs3-panel/src/routes/(staff)/server-log/+page.svelte` returns at least 1
    - `grep -c "debouncedSearch" vs3-panel/src/routes/(staff)/server-log/+page.svelte` returns at least 2
    - `grep -c "sortOrder\|Newest First\|Oldest First" vs3-panel/src/routes/(staff)/server-log/+page.svelte` returns at least 2
    - `grep -c "eventTypeFilter\|factionFilter\|nodeFilter" vs3-panel/src/routes/(staff)/server-log/+page.svelte` returns at least 3
    - `grep -c "faction_change\|node_change\|war_event\|manual_entry" vs3-panel/src/routes/(staff)/server-log/+page.svelte` returns at least 4
    - `grep -c "Add Manual Entry\|addManualEntry" vs3-panel/src/routes/(staff)/server-log/+page.svelte` returns at least 2
  </acceptance_criteria>
  <done>
    Server log shows all auto-populated entries and manual entries. Filter bar allows filtering by event type, faction, node, and text search. Sort toggle works. Add Manual Entry modal creates entry with staff username as actor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| addManualEntry description | User-supplied text must be length-limited to prevent abuse |
| actor field derivation | Must come from server auth store, not user-supplied form data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-22 | Tampering | addManualEntry actor spoofing | mitigate | Actor is set server-side from `locals.pb.authStore.record?.username` — not from form data. No actor field in the form. |
| T-02-23 | Information Disclosure | server_log full list | accept | Staff can see all server log entries — this is correct for an admin audit tool. Member portal does not have a server log page. |
| T-02-24 | Tampering | description length | mitigate | Zod validates description max 1000 chars. PocketBase Text field has no automatic max — zod is the enforcement layer. |
</threat_model>

<verification>
1. Navigate to /server-log — existing auto-populated entries from JSVM hooks appear
2. Filter by event type "Faction Change" — only faction_change entries shown
3. Click "Oldest First" — entries reorder ascending by time
4. Click "Add Manual Entry" — enter 15+ char description, select a faction — entry appears with staff username as actor
5. Filter by text search for part of the manual entry text — entry appears in filtered results
</verification>

<success_criteria>
- All auto-populated log entries visible and filterable
- Four filter dimensions (event type, faction, node, text) work independently
- Sort toggle works
- Manual entry shows staff username as actor, not "System"
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-data-wars/02-07-SUMMARY.md`
</output>

---
phase: 02-core-data-wars
plan: 04
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - vs3-panel/src/routes/(staff)/nodes/+page.svelte
  - vs3-panel/src/routes/(staff)/nodes/+page.server.ts
  - vs3-panel/src/routes/(staff)/nodes/[id]/+page.svelte
  - vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - WAR-06
  - LOG-03
  - METRICS-03

must_haves:
  truths:
    - "Staff can create nodes with all 15 type options, tier 1-4, owner faction, base upkeep, road flag, and notes"
    - "Military nodes show tier label (Watchtower/Outpost/Fort/Bastion) and free reinforcement slot count equal to tier number"
    - "Node detail shows ownership history timeline with method badges (peaceful/violent/system) and newest entry first"
    - "Transfer Ownership action creates a node_ownership_history record with correct method and updates nodes.owner"
    - "Effective upkeep on node detail is calculated at read time from owner's node count + war count — never stored"
    - "Node detail shows server log entries for that node (per-node history LOG-03)"
  artifacts:
    - path: "vs3-panel/src/routes/(staff)/nodes/+page.svelte"
      provides: "Nodes list with filter by type/tier/owner, tier badges, instability dots"
      contains: "filteredNodes"
    - path: "vs3-panel/src/routes/(staff)/nodes/+page.server.ts"
      provides: "load + createNode, deleteNode actions"
      contains: "createNode"
    - path: "vs3-panel/src/routes/(staff)/nodes/[id]/+page.svelte"
      provides: "Node detail with ownership timeline, upkeep section, node log"
      contains: "node_ownership_history"
    - path: "vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts"
      provides: "load + editNode, transferOwnership, deleteNode actions"
      contains: "transferOwnership"
  key_links:
    - from: "vs3-panel/src/routes/(staff)/nodes/[id]/+page.svelte"
      to: "vs3-panel/src/lib/upkeep.ts"
      via: "import { calcUpkeep }"
      pattern: "calcUpkeep"
    - from: "vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts"
      to: "PocketBase node_ownership_history collection"
      via: "locals.pb.collection('node_ownership_history').create()"
      pattern: "node_ownership_history"
---

<objective>
Build the Nodes section: list page (create, delete, filter by type/tier/owner) and detail page (edit, transfer ownership, view ownership timeline, view per-node log, display computed effective upkeep).

Purpose: Nodes are the core game object — every upkeep, instability, and war event references a node. This plan delivers full node CRUD and the ownership timeline that WAR-06 requires.
Output: 4 route files for node list and detail, wired to `nodes`, `node_ownership_history`, `factions`, `wars`, `server_log` collections.
</objective>

<execution_context>
@C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/execute-plan.md
</execution_context>

<context>
@C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-RESEARCH.md
@C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-PATTERNS.md
@C:/Users/Kramer\Desktop\VS3\.planning\phases\02-core-data-wars\02-UI-SPEC.md
@C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Node types (exactly 15 — from RESEARCH.md, confirmed vs VS3_Panel_1_2_1.html): -->
```typescript
const NODE_TYPES = [
  'Farm', 'Ranch', 'Orchard', 'Mine', 'Quarry', 'Clay Pit', 'Forest',
  'Lumber Mill', 'Resin Farm', 'Peat Bog', 'Salt Works', 'Workshop',
  'Trade Post', 'Military Node', 'Harbor/River Landing'
];
```

<!-- Military tier labels (CLAUDE.md): -->
```typescript
const MIL_NAMES: Record<string, string> = {
  '1': 'Watchtower', '2': 'Outpost', '3': 'Fort', '4': 'Bastion'
};
// Free slots = parseInt(tier)
```

<!-- Ownership history sort (append-only, newest first): -->
```typescript
locals.pb.collection('node_ownership_history').getFullList({
  filter: `node = "${params.id}"`,
  sort: '-transfer_date',
  expand: 'faction'
})
```

<!-- Per-node log (LOG-03): -->
```typescript
locals.pb.collection('server_log').getFullList({
  filter: `related_node = "${params.id}"`,
  sort: '-created'
})
```

<!-- Upkeep calculation requires owner's context: -->
```typescript
// Load owner faction to get its node count and war count
const ownerFaction = node.owner ? await locals.pb.collection('factions').getOne(node.owner) : null;
const ownerNodes = ownerFaction ? await locals.pb.collection('nodes').getFullList({
  filter: `owner = "${node.owner}"`, fields: 'id'
}) : [];
const ownerWars = ownerFaction ? await locals.pb.collection('wars').getFullList({
  filter: `(faction_a = "${node.owner}" || faction_b = "${node.owner}") && status = "active"`,
  fields: 'id'
}) : [];
// Then: calcUpkeep(node.base_upkeep, ownerNodes.length, ownerWars.length, ownerFaction.type, ownerFaction.is_system)
```

<!-- Tier badge styles (UI-SPEC lines 397-403): -->
```
T1: bg rgba(139,125,101,0.12), border #3d3426, text #8b7d65
T2: bg rgba(139,125,101,0.18), border rgba(139,125,101,0.3), text #a89880
T3: bg rgba(196,164,90,0.12), border rgba(196,164,90,0.22), text #c4a45a
T4: bg rgba(196,164,90,0.22), border rgba(196,164,90,0.40), text #d4b46a
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nodes List — `+page.server.ts` + `+page.svelte`</name>
  <files>
    vs3-panel/src/routes/(staff)/nodes/+page.svelte,
    vs3-panel/src/routes/(staff)/nodes/+page.server.ts
  </files>
  <read_first>
    - `C:/Users/Kramer/Desktop/VS3/vs3-panel/src/routes/(staff)/staff-management/+page.server.ts` — action pattern, zod schema, error handling
    - `C:/Users/Kramer/Desktop/VS3/vs3-panel/src/routes/(staff)/staff-management/+page.svelte` — table, modal, filter pattern
    - `C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-UI-SPEC.md` — Surface 3: Nodes List (lines 378-413)
    - `C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-PATTERNS.md` — nodes/+page.svelte section (lines 562-590)
  </read_first>
  <behavior>
    - Load returns all nodes with owner faction name and color (expand owner)
    - Filter by search (debounced 300ms), type (15 options + All), tier (T1-T4 + All), owner (faction names + Neutral Territory + All)
    - Military Node type shows tier label in parentheses after tier badge
    - InstabilityDot renders in instability column
    - createNode validates name (required), type (one of 15), tier (1|2|3|4), owner (optional faction ID), base_upkeep (optional number), has_road (bool), road_note, notes
    - deleteNode is head_admin only
  </behavior>
  <action>
**`+page.server.ts`:**

Load: fetch all nodes with `expand: 'owner'`, sort `node_number`. Return mapped array including: id, name, node_number, type, tier, owner (id+name+color from expand), instability, has_road, base_upkeep. Fetch all factions for the owner filter dropdown.

`createNode` action: zod schema — name (min 1), type (z.enum with all 15 NODE_TYPES), tier (z.enum(['1','2','3','4'])), owner (optional string), base_upkeep (optional number from string), has_road (boolean from string 'true'/'false'), road_note (optional string), notes (optional string). Create record in nodes collection.

`deleteNode` action: head_admin gate. Delete node record.

**`+page.svelte`:**

Filters: search (debounced 300ms), typeFilter ($state 'all'), tierFilter ($state 'all'), ownerFilter ($state 'all').

filteredNodes $derived applies all four filters.

Table columns (per UI-SPEC Surface 3): # | Name | Type | Tier | Owner | Instability | Road
- `#` column: node_number, text-muted
- Tier column: tier badge with correct colors (from interfaces block above). Military Node: show MIL_NAMES[tier] in parentheses: `T2 (Outpost)`
- Owner column: faction color dot + faction name, or "Neutral Territory" in text-muted
- Instability column: `<InstabilityDot level={n.instability} />` (import from $lib/components/InstabilityDot.svelte)
- Road column: "Yes" or "—"

Row click → `goto('/nodes/' + n.id)`.

Add Node modal (680px): name, node_number, type Select (15 options), tier Select (1-4), owner Select (all factions + blank/unowned), base_upkeep number input, has_road checkbox, road_note (shown only if has_road), notes textarea.

Empty state: "No nodes yet" / "Add nodes to begin tracking territory, upkeep, and instability."
  </action>
  <verify>
    <automated>cd /c/Users/Kramer/Desktop/VS3/vs3-panel && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -i "nodes/" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createNode" vs3-panel/src/routes/(staff)/nodes/+page.server.ts` returns at least 2
    - `grep -c "deleteNode" vs3-panel/src/routes/(staff)/nodes/+page.server.ts` returns at least 2
    - `grep -c "head_admin" vs3-panel/src/routes/(staff)/nodes/+page.server.ts` returns at least 1
    - `grep -c "filteredNodes" vs3-panel/src/routes/(staff)/nodes/+page.svelte` returns at least 1
    - `grep -c "InstabilityDot" vs3-panel/src/routes/(staff)/nodes/+page.svelte` returns at least 1
    - `grep -c "MIL_NAMES\|Watchtower\|Outpost\|Fort\|Bastion" vs3-panel/src/routes/(staff)/nodes/+page.svelte` returns at least 1
    - `grep -c "tierFilter\|typeFilter\|ownerFilter" vs3-panel/src/routes/(staff)/nodes/+page.svelte` returns at least 3
    - `grep -c "debouncedSearch" vs3-panel/src/routes/(staff)/nodes/+page.svelte` returns at least 2
  </acceptance_criteria>
  <done>
    Nodes list page shows all nodes with tier badges, instability dots, military tier labels. Create/delete actions work. Four-filter bar operates correctly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Node Detail — `[id]/+page.server.ts` + `[id]/+page.svelte`</name>
  <files>
    vs3-panel/src/routes/(staff)/nodes/[id]/+page.svelte,
    vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts
  </files>
  <read_first>
    - `C:/Users/Kramer/Desktop/VS3/vs3-panel/src/routes/(staff)/staff-management/+page.server.ts` — action/load patterns
    - `C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-UI-SPEC.md` — Surface 4: Node Detail (lines 416-443)
    - `C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-PATTERNS.md` — Ownership Timeline component (lines 220-228)
    - `C:/Users/Kramer/Desktop/VS3/.planning/phases/02-core-data-wars/02-RESEARCH.md` — Pattern 8 (ownership transfer two-call approach) and Pitfall 4 (race condition accepted for Phase 2)
  </read_first>
  <behavior>
    - Load fetches node, owner faction (for upkeep calc), owner's node count, owner's war count, ownership history (sort -transfer_date), per-node server log
    - Effective upkeep calculated: calcUpkeep(node.base_upkeep, ownerNodeCount, ownerWarCount, ownerFaction.type, ownerFaction.is_system)
    - transferOwnership action: (1) updates nodes.owner to new faction ID, (2) creates node_ownership_history record with method (peaceful|violent|system), transfer_date, staff_note
    - editNode action: updates all editable node fields
    - deleteNode action: head_admin only
    - Military nodes show "Free Slots: [tier]" in detail header sub-row
  </behavior>
  <action>
**`[id]/+page.server.ts`:**

Load with Promise.all:
- node: `getOne(params.id, { expand: 'owner' })`
- ownershipHistory: `node_ownership_history.getFullList({ filter: node.owner ? ... , sort: '-transfer_date', expand: 'faction' })`
- nodeLog: `server_log.getFullList({ filter: related_node = "${params.id}", sort: '-created' })`
- ownerNodes (if owner): `nodes.getFullList({ filter: owner = "${node.owner}", fields: 'id' })`
- ownerWars (if owner): `wars.getFullList({ filter: (faction_a = "${node.owner}" || faction_b = "${node.owner}") && status = "active", fields: 'id' })`
- factionList (for transfer dropdown): all factions

Return all of the above plus calculated context values (ownerNodeCount, ownerWarCount for use in $derived on client).

Actions:
- `editNode`: zod validates all node fields, updates node record.
- `transferOwnership`: **head_admin gate first** — `if (locals.pb.authStore.record?.role !== 'head_admin') return fail(403, { action: 'transferOwnership', errors: { _global: ['Head Admin access required.'] } })`. Ownership transfer is irreversible (append-only history). After gate check: zod validates newOwner (string), method (peaceful|violent|system), transfer_date (string), staff_note (optional). Sequential calls:
  1. `nodes.update(id, { owner: newOwner })`
  2. `node_ownership_history.create({ node: id, faction: newOwner, transfer_date, method, staff_note })`
  Both wrapped in try/catch independently (see RESEARCH Pitfall 4). Per CLAUDE.md §Critical Constraints #5, Head Admin must be enforced at BOTH route level AND collection rule level. The collection rule (no updateRule on node_ownership_history) is set in plan 02-01 schema task.
- `deleteNode`: head_admin gate, deletes node.

**`[id]/+page.svelte`:**

Import `{ calcUpkeep }` from `$lib/upkeep`. Compute in $derived:
```svelte
let effectiveUpkeep = $derived(
  calcUpkeep(data.node.base_upkeep, data.ownerNodeCount, data.ownerWarCount,
             data.ownerFactionType, data.ownerIsSystem)
);
```

Sections (per UI-SPEC Surface 4):
1. Back link: "← Back to Nodes"
2. Detail header card (4px left border in owner faction color or #8b7d65):
   - Node name (22px/600)
   - Sub-row: type + tier badge + owner name + instability badge
   - Military Node: "Free Slots: {tier}" label (11px/600/gold) — DATA-05
   - Action buttons: "Edit Node" (ghost), "Transfer Ownership" (ghost), "Delete Node" (danger — head_admin only)
3. "ROAD CONNECTION" section: only if has_road. "Connected via {road_note}"
4. "UPKEEP" section: "Base: {base_upkeep} SP × Overextension: ×{oeMul} × War modifier: {war mod or N/A (PvE)} = {effectiveUpkeep} SP/week". Sub-text: driving factors. Phase 2 note: "SP submission tracking coming in Phase 3."
5. "OWNERSHIP HISTORY" section: vertical timeline, newest first.
   - Each entry: dot (method color: peaceful=#90cc90, violent=#e07840, system=#8b7d65) + connector line + faction name + method badge ("Peaceful Transfer"/"Violent Conquest"/"System") + date + staff note
   - Current owner (first entry): gold dot #c4a45a + "CURRENT" label
   - Empty state: "No ownership history recorded. Assign an owner to begin the timeline."
6. "NOTES" section: only if notes non-empty. Pre-wrap text.
7. Per-node log at bottom: log entries table (time, event_type badge, description) filtered to this node

Transfer Ownership modal (680px): New Owner Select (all factions), Method Select (Peaceful/Violent/System), Date (date input, default today), Staff Note (textarea optional).
Edit Node modal (680px): all editable fields pre-filled.
Delete Node confirmation (400px).
  </action>
  <verify>
    <automated>cd /c/Users/Kramer/Desktop/VS3/vs3-panel && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -i "nodes/\[id\]" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "transferOwnership" vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts` returns at least 2
    - `grep -c "head_admin.*transferOwnership|transferOwnership.*head_admin|head_admin" vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts` returns at least 2 (one for deleteNode gate, one for transferOwnership gate)
    - `grep -c "node_ownership_history" vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts` returns at least 2 (load + create in action)
    - `grep -c "calcUpkeep" vs3-panel/src/routes/(staff)/nodes/[id]/+page.svelte` returns at least 1
    - `grep -c "transfer_date\|peaceful\|violent\|system" vs3-panel/src/routes/(staff)/nodes/[id]/+page.svelte` returns at least 3
    - `grep -c "Free Slots" vs3-panel/src/routes/(staff)/nodes/[id]/+page.svelte` returns at least 1
    - `grep -c "CURRENT" vs3-panel/src/routes/(staff)/nodes/[id]/+page.svelte` returns at least 1 (current owner indicator)
    - `grep -c "server_log" vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts` returns at least 1 (per-node log load)
    - `grep -c "sort.*-transfer_date\|-transfer_date" vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts` returns at least 1
  </acceptance_criteria>
  <done>
    Node detail shows ownership timeline (newest first with method badges), computed effective upkeep, military tier labels with free slot count, per-node server log, transfer ownership action creates history record.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transferOwnership action | Staff can transfer any node to any faction — only head_admin should be able to execute destructive transfer |
| node_ownership_history | Append-only — no updateRule set — protects audit trail integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Elevation of Privilege | deleteNode action | mitigate | `locals.pb.authStore.record?.role !== 'head_admin'` gate + PocketBase deleteRule = head_admin only |
| T-02-12 | Tampering | transferOwnership two-call race | accept | PocketBase SDK has no multi-collection transactions. If history create fails, node has correct owner (no corruption). Accepted for Phase 2 per RESEARCH Pitfall 4. |
| T-02-15 | Elevation of Privilege | transferOwnership action | mitigate | head_admin role gate added per CLAUDE.md §Critical Constraints #5. Ownership transfer is irreversible (append-only history); staff accounts must not execute it. Gate returns fail(403) before any DB operation. |
| T-02-13 | Tampering | node_ownership_history updateRule | mitigate | updateRule is empty on collection (no update permitted). History is append-only at the DB rule level. |
| T-02-14 | Tampering | owner field filter injection | mitigate | params.id is a SvelteKit route segment. PocketBase SDK parameterizes filter values. |
</threat_model>

<verification>
1. Create a node, assign it to a PvP faction with 3 total nodes and 1 active war — node detail shows effective upkeep = ceil(base × 1.2 × 1.15)
2. Transfer ownership (violent) — ownership history shows new entry with red/orange dot "Violent Conquest" at top as "CURRENT"
3. Military Node tier 3 — detail header shows "Free Slots: 3" and tier label "Fort"
4. Per-node log shows server log entries for this node (from log_hooks JSVM hook)
5. Delete node as Staff account — expect 403
</verification>

<success_criteria>
- All 15 node types available in create form
- Military nodes display correct tier name and free slot count
- Ownership timeline is append-only, newest first, with method colors
- Effective upkeep calculated at read time, never stored
- Per-node server log shown on detail page (LOG-03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-data-wars/02-04-SUMMARY.md`
</output>

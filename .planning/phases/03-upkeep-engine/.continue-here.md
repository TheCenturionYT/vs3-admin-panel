---
context: phase
phase: 03-upkeep-engine
task: 4
total_tasks: 6
status: in_progress
last_updated: 2026-05-02T00:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Orphaned worktree with uncommitted SUMMARY.md — Plan 03-04's implementation is committed but SUMMARY.md was never created/committed due to stream timeout. The worktree still exists at `.claude/worktrees/agent-ac5f1f2dca91237e0`. You MUST create and commit SUMMARY.md in that worktree before merging it.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stream timeout leaves orphaned worktree | Agent for 03-04 hit `Stream idle timeout` after completing 2 implementation commits but before writing SUMMARY.md. Worktree remains at `.claude/worktrees/agent-ac5f1f2dca91237e0` (branch `worktree-agent-ac5f1f2dca91237e0`), locked. | blocking | Before merging any worktree, verify SUMMARY.md is committed inside the worktree (not just on main). Read plan file + committed source files to construct SUMMARY.md manually if agent didn't finish. |

<current_state>
Phase 3 — Upkeep Engine & Automation execution is in progress. Wave 3 (plan 03-04) had its implementation committed successfully via a subagent executor, but the agent hit a stream idle timeout before creating SUMMARY.md. The worktree for 03-04 still exists and is locked. Wave 4 (03-05, 03-06) has not been started.

Waves completed:
- Wave 1 ✓ — 03-01 (schema migration, npm deps, shadcn components)
- Wave 2 ✓ — 03-02 (scheduler + INSTAB_EVENTS), 03-03 (server settings deadline config UI)
- Wave 3 ⚠ — 03-04 code committed, SUMMARY.md missing, worktree not yet merged
- Wave 4 ✗ — 03-05, 03-06 not started
</current_state>

<completed_work>

Completed Plans:
- Plan 03-01 (Wave 1): Phase 3 schema migration (submissions, submission_history, deadline_config, instability_rolls, job_run_log collections), svelte5-chartjs + chart.js installed, shadcn Progress + Switch components added. SUMMARY committed on main.
- Plan 03-02 (Wave 2): Full `procDeadlines()` + `jsvm_calcUp()` port in `pb_hooks/scheduler.js`, 77 INSTAB_EVENTS in `vs3-panel/src/lib/instab_events.ts`. SUMMARY committed on main.
- Plan 03-03 (Wave 2): Deadline Configuration card on `/server-settings` with live next-deadline preview, head admin edit / staff read-only. SUMMARY committed on main.
- Plan 03-04 (Wave 3, PARTIAL): Implementation committed (2 commits: `eb8e61f` server actions, `c1e2045` UI render). SUMMARY.md NOT created. Worktree at `.claude/worktrees/agent-ac5f1f2dca91237e0` on branch `worktree-agent-ac5f1f2dca91237e0`.

ROADMAP.md updated: 3/6 plans marked complete (03-01, 03-02, 03-03). 03-04 not yet marked.
</completed_work>

<remaining_work>

**Immediate (before Wave 4 can start):**
1. Create SUMMARY.md for plan 03-04 inside the worktree at `.claude/worktrees/agent-ac5f1f2dca91237e0`
   - Read: `.planning/phases/03-upkeep-engine/03-04-PLAN.md`
   - Read: `vs3-panel/src/routes/(staff)/nodes/[id]/+page.server.ts` (in worktree)
   - Read: `vs3-panel/src/routes/(staff)/nodes/[id]/+page.svelte` (in worktree)
   - Commit with `--no-verify` inside the worktree
2. Merge worktree `worktree-agent-ac5f1f2dca91237e0` into main (using standard orchestrator merge protocol)
3. Remove worktree + delete branch
4. Update ROADMAP.md (03-04 → [x], progress 4/6) and commit tracking

**Wave 4 (after 03-04 merged):**
5. Execute 03-05 (Dashboard: scheduler health card, overdue widget, quick-log modal, Process All Overdue)
6. Execute 03-06 (Metrics page: SP totals + weekly chart + sidebar link)
   - Both 03-05 and 03-06 can run in parallel (worktree isolation, no file overlap)

**Post-Wave 4:**
7. Code review gate
8. Phase verification (gsd-verifier)
9. Update ROADMAP.md to complete, update STATE.md
</remaining_work>

<decisions_made>

- Business logic ported from `Admin Panel/VS3_Panel_1_2_1.html` v1 code, not handbook prose (per CLAUDE.md constraint)
- INSTAB_EVENTS: 77 entries total (v1 has 77, not 75 as plan prose suggested — Military Node has 7 events). All ported verbatim.
- Scheduler uses `$app.runInTransaction` for atomic idempotency, `last_processed_ts` as idempotency gate
- Pre-existing type errors in `wars/[id]/+page.svelte` (86 errors from Phase 2) — not introduced by Phase 3 work, do not block Wave 4 execution
- Modified SUMMARY.md files on main (01-01 through 01-06, 03-02, 03-03, research/SUMMARY.md) have line-ending drift from rescue copies — not blocking
</decisions_made>

<blockers>
- Plan 03-04 worktree must be finalized before Wave 4 can start (SUMMARY.md missing, not merged)
- Worktree lock PID 37640 may be stale — use `git worktree unlock` then `git worktree remove --force --force` if needed
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-upkeep-engine/03-04-PLAN.md` — must-haves for 03-04, including all server actions and UI requirements
2. `.planning/phases/03-upkeep-engine/03-05-PLAN.md` — Wave 4 plan A
3. `.planning/phases/03-upkeep-engine/03-06-PLAN.md` — Wave 4 plan B
4. `./CLAUDE.md` — upkeep formula, cap rules, instability scale (all must be respected)
5. `.planning/phases/03-upkeep-engine/03-02-SUMMARY.md` — INSTAB_EVENTS structure and instab_events.ts exports (03-04 and 03-05 depend on this)

## Infrastructure State
- PocketBase binary: `pocketbase.exe` at repo root (not running during execution — migration applied on next start)
- Git worktree for 03-04: `.claude/worktrees/agent-ac5f1f2dca91237e0` — locked, contains 2 committed but unmerged feat commits
- Current branch: `master`
- No dev server running

<context>
Was executing Phase 3 with wave-based parallel subagent execution. Wave 1 and Wave 2 completed cleanly. Wave 3 (03-04) hit a stream idle timeout after the agent completed 2 implementation commits (server actions + UI render) but before the final SUMMARY.md step. The implementation is done — only the SUMMARY.md document is missing.

Next session should: (1) manually create and commit SUMMARY.md for 03-04 in the existing worktree, (2) run the standard worktree merge protocol, (3) then spawn Wave 4 agents in parallel (03-05 and 03-06 touch different files — no overlap).
</context>

<next_action>
Start with: cd into worktree at `.claude/worktrees/agent-ac5f1f2dca91237e0`, read `03-04-PLAN.md` and the two committed source files (`+page.server.ts` and `+page.svelte`), write SUMMARY.md, commit it with `--no-verify`. Then run the orchestrator merge protocol and proceed to Wave 4.
</next_action>
